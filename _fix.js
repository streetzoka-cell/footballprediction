var fs = require("fs");
var path = require("path");
var F = "C:\\Users\\COISA COMPUTERS\\Desktop\\Apk\\footballprediction";
var B = F + "\\football-data-backend";
function w(p, c) { fs.mkdirSync(path.dirname(p), { recursive: true }); fs.writeFileSync(p, c, "utf8"); console.log("  + " + p.replace(F + "\\", "")); }

console.log("[1/9] Backend: utils/memoryCache.js");
w(B + "\\utils\\memoryCache.js", "class MemoryCache {\n  constructor() {\n    this.store = new Map();\n  }\n  get(key, ttlMs) {\n    ttlMs = ttlMs || 30000;\n    var entry = this.store.get(key);\n    if (!entry) return null;\n    if (Date.now() - entry.ts > ttlMs) {\n      this.store.delete(key);\n      return null;\n    }\n    return entry.data;\n  }\n  set(key, data) {\n    this.store.set(key, { data: data, ts: Date.now() });\n  }\n  del(key) {\n    this.store.delete(key);\n  }\n  clear() {\n    this.store.clear();\n  }\n  size() {\n    return this.store.size;\n  }\n}\n\nmodule.exports = new MemoryCache();\n");

console.log("[2/9] Backend: services/matches.js");
w(B + "\\services\\matches.js", "const api = require(\"../config/api\");\nconst cache = require(\"../utils/cache\");\nconst retry = require(\"../utils/retry\");\nconst logger = require(\"../utils/logger\");\n\nconst FIXTURES_COL = \"fixtures\";\nconst LIVE_COL = \"liveMatches\";\n\nfunction v(x) { return (x !== undefined && x !== null) ? x : null; }\n\nfunction normaliseMatch(m) {\n  var score = m.score || {};\n  var goals = (score.goals || []).map(function(g) {\n    return {\n      team: g.team ? { id: v(g.team.id), name: v(g.team.name) } : null,\n      scorer: g.scorer ? { id: v(g.scorer.id), name: v(g.scorer.name) } : null,\n      assist: g.assist ? { id: v(g.assist.id), name: v(g.assist.name) } : null,\n      minute: v(g.minute),\n      type: v(g.type),\n      extraTime: !!g.extraTime,\n      penaltyShootout: !!g.penaltyShootout,\n    };\n  });\n  var cards = (score.cards || []).map(function(c) {\n    return {\n      team: c.team ? { id: v(c.team.id), name: v(c.team.name) } : null,\n      player: c.player ? { id: v(c.player.id), name: v(c.player.name) } : null,\n      minute: v(c.minute),\n      type: v(c.type),\n    };\n  });\n  var referees = (m.referees || []).map(function(r) {\n    return { id: v(r.id), name: v(r.name), role: v(r.role), nationality: v(r.nationality) };\n  });\n\n  return {\n    id: String(m.id),\n    date: m.utcDate ? m.utcDate.split(\"T\")[0] : null,\n    utcDate: v(m.utcDate),\n    status: m.status || \"SCHEDULED\",\n    matchday: v(m.matchday),\n    competition: m.competition ? {\n      id: m.competition.id, name: m.competition.name,\n      code: v(m.competition.code), emblem: v(m.competition.emblem), type: v(m.competition.type),\n    } : null,\n    season: m.season ? { id: m.season.id } : null,\n    homeTeam: m.homeTeam ? {\n      id: m.homeTeam.id, name: m.homeTeam.name,\n      shortName: v(m.homeTeam.shortName), crest: v(m.homeTeam.crest),\n    } : null,\n    awayTeam: m.awayTeam ? {\n      id: m.awayTeam.id, name: m.awayTeam.name,\n      shortName: v(m.awayTeam.shortName), crest: v(m.awayTeam.crest),\n    } : null,\n    score: {\n      halfTime:  { home: v(score.halfTime ? score.halfTime.home : null),  away: v(score.halfTime ? score.halfTime.away : null) },\n      fullTime:  { home: v(score.fullTime ? score.fullTime.home : null),  away: v(score.fullTime ? score.fullTime.away : null) },\n      extraTime: { home: v(score.extraTime ? score.extraTime.home : null), away: v(score.extraTime ? score.extraTime.away : null) },\n      penalties: { home: v(score.penalties ? score.penalties.home : null), away: v(score.penalties ? score.penalties.away : null) },\n      winner: v(score.winner),\n      duration: v(score.duration),\n      goals: goals,\n      cards: cards,\n      corners: score.corners ? { home: v(score.corners.home), away: v(score.corners.away) } : null,\n    },\n    referees: referees,\n    fetchedAt: new Date().toISOString(),\n  };\n}\n\nfunction getDateStr(offset) {\n  var d = new Date(); d.setDate(d.getDate() + offset);\n  return d.toISOString().split(\"T\")[0];\n}\n\nasync function fetchTodayFromApi() {\n  logger.debug(\"[MATCHES] Fetching today ...\");\n  var r = await retry(function() { return api.get(\"/matches\"); }, { label: \"matches-today\" });\n  return (r.data.matches || []).map(normaliseMatch);\n}\n\nasync function fetchRangeFromApi(dateFrom, dateTo) {\n  logger.info(\"[MATCHES] Range \" + dateFrom + \" to \" + dateTo + \" ...\");\n  var r = await retry(function() { return api.get(\"/matches\", { params: { dateFrom: dateFrom, dateTo: dateTo } }); }, { label: \"range-\" + dateFrom });\n  return (r.data.matches || []).map(normaliseMatch);\n}\n\nasync function cacheTodayMatches() {\n  var matches = await fetchTodayFromApi();\n  var live = matches.filter(function(m) { return m.status === \"IN_PLAY\" || m.status === \"PAUSED\"; });\n  if (matches.length) await cache.batchSet(FIXTURES_COL, matches.map(function(m) { return { id: m.id, data: m }; }));\n  await cache.replaceCollection(LIVE_COL, live.map(function(m) { return { id: m.id, data: m }; }));\n  await cache.setLastUpdated(\"liveMatches\", { count: live.length });\n  logger.info(\"[MATCHES] Today: \" + matches.length + \" total, \" + live.length + \" live\");\n  return { matches: matches, live: live };\n}\n\nasync function cacheDateRange() {\n  var fp = getDateStr(-8), td = getDateStr(0), ff = getDateStr(1), tf = getDateStr(8);\n  logger.info(\"[MATCHES] 16-day range (two batches) ...\");\n  var past = await fetchRangeFromApi(fp, td);\n  logger.info(\"[MATCHES] Past 8: \" + past.length);\n  if (past.length) await cache.batchSet(FIXTURES_COL, past.map(function(m) { return { id: m.id, data: m }; }));\n  var future = await fetchRangeFromApi(ff, tf);\n  logger.info(\"[MATCHES] Next 8: \" + future.length);\n  if (future.length) await cache.batchSet(FIXTURES_COL, future.map(function(m) { return { id: m.id, data: m }; }));\n  var total = past.length + future.length;\n  await cache.setLastUpdated(\"fixturesRange\", { count: total });\n  logger.info(\"[MATCHES] Range: \" + total + \" total\");\n  return total;\n}\n\nasync function getCachedAll() {\n  var all = await cache.getCollection(FIXTURES_COL);\n  return all.map(function(r) { return r.data; });\n}\n\nasync function getCachedByDateRange(dateFrom, dateTo) {\n  var all = await cache.getCollection(FIXTURES_COL);\n  return all.map(function(r) { return r.data; }).filter(function(m) { return m.date && m.date >= dateFrom && m.date <= dateTo; });\n}\nasync function getCachedToday() { var t = getDateStr(0); return getCachedByDateRange(t, t); }\nasync function getCachedLive() { var r = await cache.getCollection(LIVE_COL); return r.map(function(r) { return r.data; }); }\nasync function getCachedFinished() { var a = await cache.getCollection(FIXTURES_COL); return a.map(function(r) { return r.data; }).filter(function(m) { return m.status === \"FINISHED\"; }); }\n\nmodule.exports = {\n  cacheTodayMatches: cacheTodayMatches,\n  cacheDateRange: cacheDateRange,\n  getCachedAll: getCachedAll,\n  getCachedByDateRange: getCachedByDateRange,\n  getCachedToday: getCachedToday,\n  getCachedLive: getCachedLive,\n  getCachedFinished: getCachedFinished,\n  fetchTodayFromApi: fetchTodayFromApi,\n  normaliseMatch: normaliseMatch,\n  getDateStr: getDateStr\n};\n");

console.log("[3/9] Backend: routes/matches.js");
w(B + "\\routes\\matches.js", "const express = require(\"express\");\nconst router = express.Router();\nconst matchesService = require(\"../services/matches\");\nconst liveService = require(\"../services/liveMatches\");\nconst finishedService = require(\"../services/finishedMatches\");\nconst cache = require(\"../utils/cache\");\nconst memCache = require(\"../utils/memoryCache\");\nconst logger = require(\"../utils/logger\");\n\nrouter.get(\"/live\", async (req, res) => {\n  try {\n    var data = memCache.get(\"live\", 15000);\n    if (!data) {\n      data = await liveService.getCached();\n      memCache.set(\"live\", data);\n    }\n    var meta = memCache.get(\"meta:live\", 60000) || await cache.getLastUpdated(\"liveMatches\");\n    memCache.set(\"meta:live\", meta);\n    res.json({ data: data, cached: true, lastUpdated: meta ? meta.timestamp : null });\n  } catch (err) {\n    logger.error(\"[ROUTE] /api/live error: \" + err.message);\n    res.status(500).json({ error: \"Failed to fetch live matches\" });\n  }\n});\n\nrouter.get(\"/today\", async (req, res) => {\n  try {\n    var data = memCache.get(\"today\", 20000);\n    if (!data) {\n      data = await matchesService.getCachedToday();\n      memCache.set(\"today\", data);\n    }\n    var meta = memCache.get(\"meta:live\", 60000) || await cache.getLastUpdated(\"liveMatches\");\n    memCache.set(\"meta:live\", meta);\n    res.json({ data: data, cached: true, lastUpdated: meta ? meta.timestamp : null });\n  } catch (err) {\n    logger.error(\"[ROUTE] /api/today error: \" + err.message);\n    res.status(500).json({ error: \"Failed to fetch today fixtures\" });\n  }\n});\n\nrouter.get(\"/finished\", async (req, res) => {\n  try {\n    var data = memCache.get(\"finished\", 30000);\n    if (!data) {\n      data = await finishedService.getCached();\n      memCache.set(\"finished\", data);\n    }\n    var meta = memCache.get(\"meta:finished\", 60000) || await cache.getLastUpdated(\"finishedFixtures\");\n    memCache.set(\"meta:finished\", meta);\n    res.json({ data: data, cached: true, lastUpdated: meta ? meta.timestamp : null });\n  } catch (err) {\n    logger.error(\"[ROUTE] /api/finished error: \" + err.message);\n    res.status(500).json({ error: \"Failed to fetch finished matches\" });\n  }\n});\n\nrouter.get(\"/fixtures\", async (req, res) => {\n  try {\n    var data = memCache.get(\"fixtures\", 30000);\n    if (!data) {\n      data = await matchesService.getCachedAll();\n      memCache.set(\"fixtures\", data);\n    }\n    var meta = memCache.get(\"meta:fixturesRange\", 60000) || await cache.getLastUpdated(\"fixturesRange\");\n    memCache.set(\"meta:fixturesRange\", meta);\n    res.json({ data: data, cached: true, lastUpdated: meta ? meta.timestamp : null });\n  } catch (err) {\n    logger.error(\"[ROUTE] /api/fixtures error: \" + err.message);\n    res.status(500).json({ error: \"Failed to fetch fixtures\" });\n  }\n});\n\nmodule.exports = router;\n");

console.log("[4/9] Backend: routes/standings.js");
w(B + "\\routes\\standings.js", "const express = require(\"express\");\nconst router = express.Router();\nconst standingsService = require(\"../services/standings\");\nconst cache = require(\"../utils/cache\");\nconst memCache = require(\"../utils/memoryCache\");\nconst logger = require(\"../utils/logger\");\n\nrouter.get(\"/:competition\", async (req, res) => {\n  try {\n    var competition = req.params.competition;\n    var force = req.query.force === \"true\";\n    var cacheKey = \"standings:\" + competition;\n\n    if (force) {\n      memCache.del(cacheKey);\n      logger.info(\"[ROUTE] /api/standings/\" + competition + \" - force refresh\");\n      var all = await standingsService.fetchAndCacheAll();\n      var result = all[competition];\n      if (!result) return res.status(404).json({ error: \"No standings found for \" + competition });\n      memCache.set(cacheKey, result);\n      return res.json({ data: result, cached: false, lastUpdated: new Date().toISOString(), source: \"api\" });\n    }\n\n    var data = memCache.get(cacheKey, 600000);\n    if (!data) {\n      data = await standingsService.getCached(competition);\n      if (data) memCache.set(cacheKey, data);\n    }\n\n    if (!data) {\n      return res.status(404).json({ error: \"No cached standings for \\\"\" + competition + \"\\\".\" });\n    }\n\n    var meta = memCache.get(\"meta:standings\", 60000) || await cache.getLastUpdated(\"standings\");\n    memCache.set(\"meta:standings\", meta);\n\n    res.json({\n      data: data.standings,\n      competitionCode: data.competitionCode,\n      cached: true,\n      lastUpdated: data.fetchedAt || (meta ? meta.timestamp : null),\n      source: \"cache\",\n    });\n  } catch (err) {\n    logger.error(\"[ROUTE] /api/standings/\" + req.params.competition + \" error: \" + err.message);\n    res.status(500).json({ error: \"Failed to fetch standings\" });\n  }\n});\n\nmodule.exports = router;\n");

console.log("[5/9] Backend: routes/teams.js");
w(B + "\\routes\\teams.js", "const express = require(\"express\");\nconst router = express.Router();\nconst teamsService = require(\"../services/teams\");\nconst cache = require(\"../utils/cache\");\nconst memCache = require(\"../utils/memoryCache\");\nconst logger = require(\"../utils/logger\");\n\nrouter.get(\"/:competition\", async (req, res) => {\n  try {\n    var competition = req.params.competition;\n    var force = req.query.force === \"true\";\n    var cacheKey = \"teams:\" + competition;\n\n    if (force) {\n      memCache.del(cacheKey);\n      logger.info(\"[ROUTE] /api/teams/\" + competition + \" - force refresh\");\n      var all = await teamsService.fetchAndCacheAll();\n      var result = all[competition];\n      if (!result) return res.status(404).json({ error: \"No teams found for \" + competition });\n      memCache.set(cacheKey, result);\n      return res.json({ data: result, cached: false, lastUpdated: new Date().toISOString(), source: \"api\" });\n    }\n\n    var data = memCache.get(cacheKey, 600000);\n    if (!data) {\n      data = await teamsService.getCached(competition);\n      if (data) memCache.set(cacheKey, data);\n    }\n\n    if (!data) {\n      return res.status(404).json({ error: \"No cached teams for \\\"\" + competition + \"\\\".\" });\n    }\n\n    var meta = memCache.get(\"meta:teams\", 60000) || await cache.getLastUpdated(\"teams\");\n    memCache.set(\"meta:teams\", meta);\n\n    res.json({\n      data: data.teams,\n      competitionCode: data.competitionCode,\n      cached: true,\n      lastUpdated: data.fetchedAt || (meta ? meta.timestamp : null),\n      source: \"cache\",\n    });\n  } catch (err) {\n    logger.error(\"[ROUTE] /api/teams/\" + req.params.competition + \" error: \" + err.message);\n    res.status(500).json({ error: \"Failed to fetch teams\" });\n  }\n});\n\nmodule.exports = router;\n");

console.log("[6/9] Backend: routes/competitions.js");
w(B + "\\routes\\competitions.js", "const express = require(\"express\");\nconst router = express.Router();\nconst competitionsService = require(\"../services/competitions\");\nconst cache = require(\"../utils/cache\");\nconst memCache = require(\"../utils/memoryCache\");\nconst logger = require(\"../utils/logger\");\n\nrouter.get(\"/\", async (req, res) => {\n  try {\n    var force = req.query.force === \"true\";\n\n    if (force) {\n      memCache.del(\"competitions\");\n      logger.info(\"[ROUTE] /api/competitions - force refresh\");\n      var data = await competitionsService.fetchAndCache();\n      memCache.set(\"competitions\", data);\n      return res.json({ data: data, cached: false, lastUpdated: new Date().toISOString(), source: \"api\" });\n    }\n\n    var data = memCache.get(\"competitions\", 3600000);\n    if (!data) {\n      data = await competitionsService.getCached();\n      memCache.set(\"competitions\", data);\n    }\n\n    var meta = memCache.get(\"meta:competitions\", 60000) || await cache.getLastUpdated(\"competitions\");\n    memCache.set(\"meta:competitions\", meta);\n\n    res.json({\n      data: data,\n      cached: true,\n      lastUpdated: meta ? meta.timestamp : null,\n      source: \"cache\",\n    });\n  } catch (err) {\n    logger.error(\"[ROUTE] /api/competitions error: \" + err.message);\n    res.status(500).json({ error: \"Failed to fetch competitions\" });\n  }\n});\n\nmodule.exports = router;\n");

console.log("[7/9] Frontend: src/services/footballApi.js");
w(F + "\\src\\services\\footballApi.js", "var BASE_URL = (typeof import.meta !== \"undefined\" && import.meta.env && import.meta.env.VITE_FOOTBALL_API_URL) || \"http://localhost:3001/api\";\n\nfunction request(path, options) {\n  options = options || {};\n  var controller = new AbortController();\n  var timeout = options.timeout || 10000;\n  var timer = setTimeout(function() { controller.abort(); }, timeout);\n\n  return fetch(BASE_URL + path, {\n    method: options.method || \"GET\",\n    headers: Object.assign({ \"Accept\": \"application/json\" }, options.headers || {}),\n    signal: controller.signal,\n    body: options.body || null,\n  })\n  .then(function(res) {\n    clearTimeout(timer);\n    if (!res.ok) throw new Error(\"API \" + res.status + \": \" + res.statusText);\n    return res.json();\n  })\n  .catch(function(err) {\n    clearTimeout(timer);\n    throw err;\n  });\n}\n\nexport var footballApi = {\n  getFixtures: function() { return request(\"/fixtures\"); },\n  getLive: function() { return request(\"/live\"); },\n  getToday: function() { return request(\"/today\"); },\n  getFinished: function() { return request(\"/finished\"); },\n  getCompetitions: function() { return request(\"/competitions\"); },\n  getStandings: function(code) { return request(\"/standings/\" + code); },\n  getTeams: function(code) { return request(\"/teams/\" + code); },\n  getHealth: function() { return request(\"/health\"); },\n};\n");

console.log("[8/9] Frontend: src/utils/dates.js");
w(F + "\\src\\utils\\dates.js", "export function getLocalDateStr(offset) {\n  var d = new Date();\n  d.setDate(d.getDate() + offset);\n  var y = d.getFullYear();\n  var m = String(d.getMonth() + 1).padStart(2, \"0\");\n  var day = String(d.getDate()).padStart(2, \"0\");\n  return y + \"-\" + m + \"-\" + day;\n}\n\nexport function getLocalDateFromUtc(utcDateStr) {\n  if (!utcDateStr) return null;\n  var d = new Date(utcDateStr);\n  var y = d.getFullYear();\n  var m = String(d.getMonth() + 1).padStart(2, \"0\");\n  var day = String(d.getDate()).padStart(2, \"0\");\n  return y + \"-\" + m + \"-\" + day;\n}\n\nexport function formatDateShort(dateStr) {\n  var parts = dateStr.split(\"-\");\n  var date = new Date(Number(parts[0]), Number(parts[1]) - 1, Number(parts[2]));\n  return date.toLocaleDateString(\"en-GB\", {\n    weekday: \"short\",\n    day: \"numeric\",\n    month: \"short\",\n  });\n}\n\nexport function isToday(dateStr) {\n  return dateStr === getLocalDateStr(0);\n}\n\nexport function isYesterday(dateStr) {\n  return dateStr === getLocalDateStr(-1);\n}\n\nexport function isTomorrow(dateStr) {\n  return dateStr === getLocalDateStr(1);\n}\n\nexport function relativeDateLabel(dateStr) {\n  if (isToday(dateStr)) return \"Today\";\n  if (isYesterday(dateStr)) return \"Yesterday\";\n  if (isTomorrow(dateStr)) return \"Tomorrow\";\n  return formatDateShort(dateStr);\n}\n");

console.log("[9/9] Frontend: src/context/FootballDataContext.jsx");
w(F + "\\src\\context\\FootballDataContext.jsx", "import { createContext, useContext, useState, useEffect, useRef, useCallback, useMemo } from \"react\";\nimport { footballApi } from \"../services/footballApi\";\nimport { getLocalDateFromUtc } from \"../utils/dates\";\n\nconst FootballDataContext = createContext(null);\n\nexport function FootballDataProvider({ children }) {\n  const [fixtures, setFixtures] = useState([]);\n  const [liveMatches, setLiveMatches] = useState([]);\n  const [competitions, setCompetitions] = useState([]);\n  const [standings, setStandings] = useState({});\n  const [teams, setTeams] = useState({});\n  const [loading, setLoading] = useState(true);\n  const [lastUpdated, setLastUpdated] = useState(null);\n  const [error, setError] = useState(null);\n  const mountedRef = useRef(true);\n\n  useEffect(() => {\n    mountedRef.current = true;\n    return () => { mountedRef.current = false; };\n  }, []);\n\n  const fetchFixtures = useCallback(async () => {\n    try {\n      const res = await footballApi.getFixtures();\n      if (mountedRef.current) {\n        setFixtures(res.data || []);\n        if (res.lastUpdated) setLastUpdated(res.lastUpdated);\n        setError(null);\n      }\n    } catch (e) {\n      console.error(\"[FootballData] Fixtures error:\", e.message);\n      if (mountedRef.current) setError(prev => prev || \"fixtures\");\n    }\n  }, []);\n\n  const fetchLive = useCallback(async () => {\n    try {\n      const res = await footballApi.getLive();\n      if (mountedRef.current) setLiveMatches(res.data || []);\n    } catch (e) {\n      console.error(\"[FootballData] Live error:\", e.message);\n    }\n  }, []);\n\n  const fetchCompetitions = useCallback(async () => {\n    try {\n      const res = await footballApi.getCompetitions();\n      if (mountedRef.current) setCompetitions(res.data || []);\n    } catch (e) {\n      console.error(\"[FootballData] Competitions error:\", e.message);\n    }\n  }, []);\n\n  useEffect(() => {\n    let cancelled = false;\n    async function init() {\n      await Promise.all([fetchFixtures(), fetchLive(), fetchCompetitions()]);\n      if (!cancelled) setLoading(false);\n    }\n    init();\n    return () => { cancelled = true; };\n  }, [fetchFixtures, fetchLive, fetchCompetitions]);\n\n  useEffect(() => {\n    const timer = setInterval(fetchFixtures, 5 * 60 * 1000);\n    return () => clearInterval(timer);\n  }, [fetchFixtures]);\n\n  useEffect(() => {\n    const timer = setInterval(fetchLive, 30000);\n    return () => clearInterval(timer);\n  }, [fetchLive]);\n\n  const getStandings = useCallback(async (code) => {\n    if (standings[code]) return standings[code];\n    try {\n      const res = await footballApi.getStandings(code);\n      if (res.data) {\n        const data = { standings: res.data, competitionCode: code, fetchedAt: res.lastUpdated };\n        if (mountedRef.current) setStandings(prev => Object.assign({}, prev, { [code]: data }));\n        return data;\n      }\n    } catch (e) {\n      console.error(\"[FootballData] Standings error for \" + code + \":\", e.message);\n    }\n    return null;\n  }, [standings]);\n\n  const getTeams = useCallback(async (code) => {\n    if (teams[code]) return teams[code];\n    try {\n      const res = await footballApi.getTeams(code);\n      if (res.data) {\n        const data = { teams: res.data, competitionCode: code, fetchedAt: res.lastUpdated };\n        if (mountedRef.current) setTeams(prev => Object.assign({}, prev, { [code]: data }));\n        return data;\n      }\n    } catch (e) {\n      console.error(\"[FootballData] Teams error for \" + code + \":\", e.message);\n    }\n    return null;\n  }, [teams]);\n\n  const fixturesByDate = useCallback(() => {\n    const groups = {};\n    for (const f of fixtures) {\n      const localDate = getLocalDateFromUtc(f.utcDate);\n      if (!localDate) continue;\n      if (!groups[localDate]) groups[localDate] = [];\n      groups[localDate].push(f);\n    }\n    return Object.entries(groups).sort((a, b) => a[0].localeCompare(b[0]));\n  }, [fixtures]);\n\n  const value = {\n    fixtures,\n    liveMatches,\n    competitions,\n    standings,\n    teams,\n    loading,\n    lastUpdated,\n    error,\n    getStandings,\n    getTeams,\n    fixturesByDate,\n    refreshFixtures: fetchFixtures,\n    refreshLive: fetchLive,\n  };\n\n  return (\n    <FootballDataContext.Provider value={value}>\n      {children}\n    </FootballDataContext.Provider>\n  );\n}\n\nexport function useFootballData() {\n  const ctx = useContext(FootballDataContext);\n  if (!ctx) throw new Error(\"useFootballData must be used within FootballDataProvider\");\n  return ctx;\n}\n\nexport default FootballDataContext;\n");

console.log("\n=== Patching existing files ===");

var envPath = B + "\\.env";
if (fs.existsSync(envPath)) {
  var envContent = fs.readFileSync(envPath, "utf8");
  var envBefore = envContent;
  envContent = envContent.replace(/SUPPORTED_COMPETITIONS=.*/ , "SUPPORTED_COMPETITIONS=PL,BL1,SA,PD,FL1,CL");
  if (envContent !== envBefore) {
    fs.writeFileSync(envPath, envContent);
    console.log("  ~ football-data-backend/.env (removed EL)");
  } else {
    console.log("  . football-data-backend/.env (already correct)");
  }
}

var fenvPath = F + "\\.env";
if (fs.existsSync(fenvPath)) {
  var fenv = fs.readFileSync(fenvPath, "utf8");
  if (fenv.indexOf("VITE_FOOTBALL_API_URL") === -1) {
    fenv = fenv.trimEnd() + "\nVITE_FOOTBALL_API_URL=http://localhost:3001/api\n";
    fs.writeFileSync(fenvPath, fenv);
    console.log("  ~ .env (added VITE_FOOTBALL_API_URL)");
  } else {
    console.log("  . .env (VITE_FOOTBALL_API_URL already exists)");
  }
}

var mgPath = F + "\\src\\pages\\MasterGames.jsx";
if (fs.existsSync(mgPath)) {
  var mg = fs.readFileSync(mgPath, "utf8");
  var orig = mg;

  mg = mg.replace(
    "import { useFootballData } from '../context/FootballDataContext';",
    "import { useFootballData } from '../context/FootballDataContext';\nimport { getLocalDateStr, getLocalDateFromUtc, formatDateShort, relativeDateLabel } from '../utils/dates';"
  );

  mg = mg.replace(/^function getDateStr\b[\s\S]*?^}/gm, "");
  mg = mg.replace(/^function formatDateShort\b[\s\S]*?^}/gm, "");

  mg = mg.replace(/useState\(getDateStr\(0\)\)/, "useState(getLocalDateStr(0))");
  mg = mg.replace(/const todayStr = getDateStr\(0\);/, "const todayStr = getLocalDateStr(0);");
  mg = mg.replace(/const yesterdayStr = getDateStr\(-1\);/, "const yesterdayStr = getLocalDateStr(-1);");
  mg = mg.replace(/const tomorrowStr = getDateStr\(1\);/, "const tomorrowStr = getLocalDateStr(1);");

  mg = mg.replace(
    /const otherDates = useMemo\(\(\) => \{[\s\S]*?return arr;[\s\S]*?\}, \[\]\);/,
    "const otherDates = useMemo(() => {\n    const arr = [];\n    for (let i = -7; i <= 7; i++) {\n      if (i >= -1 && i <= 1) continue;\n      const str = getLocalDateStr(i);\n      arr.push({ str: str, label: formatDateShort(str) });\n    }\n    return arr;\n  }, []);"
  );

  mg = mg.replace(
    /const mDate = m\.utcDate \? m\.utcDate\.split\('T'\)\[0\] : '';\s*return mDate === selectedDate;/,
    "const mLocalDate = getLocalDateFromUtc(m.utcDate);\n      return mLocalDate === selectedDate;"
  );

  if (mg !== orig) {
    fs.writeFileSync(mgPath, mg);
    console.log("  ~ src/pages/MasterGames.jsx (timezone fix applied)");
  } else {
    console.log("  . src/pages/MasterGames.jsx (no changes needed)");
  }
} else {
  console.log("  ! src/pages/MasterGames.jsx not found");
}

console.log("\n=== ALL DONE ===");
